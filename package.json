{
  "name": "lough-5-site",
  "version": "1.0.0",
  "description": "A simple website for a local charity 5K race",
  "main": "index.html",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r index.html location.html course.html results.html fastest-50.html assets dist/",
    "dev": "browser-sync start --server --files '*.html, assets/**/*'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "charity",
    "race",
    "5k",
    "running",
    "alpine.js"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "serve": "^14.2.1"
  },
  "dependencies": {
    "alpinejs": "^3.13.0"
  }
}